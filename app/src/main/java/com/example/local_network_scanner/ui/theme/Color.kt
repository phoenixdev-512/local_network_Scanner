package com.example.local_network_scanner.ui.theme

import androidx.compose.ui.graphics.Color

// Legacy colors (kept for compatibility)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Modern Minimalist Color Palette - Refined for cleaner aesthetic
// Primary colors - Deep navy to electric blue gradients
val DeepNavy = Color(0xFF0B1929)        // Slightly lighter for better contrast
val ElectricBlue = Color(0xFF2196F3)     // Bright, vibrant blue
val PrimaryBlue = Color(0xFF1E88E5)

// Accent colors - Refined for better visibility
val VibrантGreen = Color(0xFF00E676)     // Brighter green for better visibility
val WarningOrange = Color(0xFFFF9800)    // Slightly softer orange
val ThreatRed = Color(0xFFF44336)        // Material red for consistency
val InfoCyan = Color(0xFF00BCD4)

// Background colors - Optimized for minimalist design
val TrueBlack = Color(0xFF000000)
val DarkBackground = Color(0xFF0F1419)   // Slightly lighter than pure black
val SurfaceDarkGray = Color(0xFF1A1F28)  // Lighter surface for better content visibility
val CardBackground = Color(0xFF252B36)   // Lighter card background for depth

// Text colors - Enhanced for readability
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFB8C1D0)    // Lighter secondary text
val TextTertiary = Color(0xFF8A93A2)     // Medium tertiary text

// Status colors
val StatusExcellent = Color(0xFF00E676)
val StatusGood = Color(0xFF76FF03)
val StatusFair = Color(0xFFFFD54F)
val StatusPoor = Color(0xFFFF6E40)

// Gradient colors - Smoother gradients
val GradientStart = Color(0xFF0B1929)
val GradientMiddle = Color(0xFF1A2332)
val GradientEnd = Color(0xFF000000)